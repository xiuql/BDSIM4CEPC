! some beamline parameters

option, beampipeRadius = 20 * cm,
        boxSize = 1.9 * m,
	tunnelRadius= 2.0 * m,
	beampipeThickness = 1 * cm;
	

! beamline definiton


qd: quadrupole,l=1000 * m;
d: drift,l=100 * m;
s1: spoiler;
a1: absorber;
endl:marker;

test_line : line = (qd,d,s1,d,a1,d,endl);
