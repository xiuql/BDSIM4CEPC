!default beam pipe radius is huge so it basically doesn't affect anything
option, beampipeRadius = 50 * cm,
        boxSize = 2.0 * m,
        tunnelRadius= 2.0 * m,
        beampipeThickness = 0.5 * cm,
        thresholdCutCharged = 10 * GeV,
        thresholdCutPhotons = 10 * GeV,
	stopTracks=1,
	nturns=2;