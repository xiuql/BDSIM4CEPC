! standard options for runing bdsim

option, beampipeRadius = 10 * cm,
        boxSize = 1.9 * m,
        tunnelRadius= 2.0 * m,
        beampipeThickness = 1 * cm,
	chordStepMinimum = 1e-9 * m,
        deltaIntersection = 0.2e-8 * m,
        deltaChord = 1e-6 * m,
        lengthSafety = 0.0 * m,

        thresholdCutCharged = 100 * MeV,
        thresholdCutPhotons = 1000 * KeV;


option,turnInteractions=1;
option,ngenerate=10;
